namespace Listener
{
    // server to client
    public enum ServerPackets
    {
        usernameReceived = 1
    }

    // client to server
    public enum ClientPackets
    {
        username = 1
    }
}
